int main() {
    int status = 1;
    int count = 10;

    if (status == 1 && count >= 10) {
        float avg = 5.5;

        if (avg < 5.0 || avg > 6.0) {
            return 0;
        } else {
            return 1;
        }
    } else {
        if (count < 0) {
            return -1;
        }
    }

    return 2;
}
