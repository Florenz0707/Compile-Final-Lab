const int MAX = 100;
int result = 0;

void calc(int n) {
    if (n > 0)
        if (n < MAX)
            result = result + n * 2;
        else
            result = 0;
    return;
}

int main() {
    int i = 0;
    while (i < 5) {
        calc(i);
        i = i + 1;
    }
    return result;
}